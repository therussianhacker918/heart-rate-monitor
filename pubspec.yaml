name: heart_rate_comparison
description: Compare heart rate algorithms from Python, React Native, and Flutter styles
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  camera: ^0.10.5+5
  permission_handler: ^11.0.1
  torch_light: ^1.0.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
